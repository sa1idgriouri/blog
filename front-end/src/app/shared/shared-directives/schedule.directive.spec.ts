import { ScheduleDirective } from './schedule.directive';

describe('ScheduleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScheduleDirective();
    expect(directive).toBeTruthy();
  });
});
